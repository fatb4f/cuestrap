# LT-01 evidence boundary

This fixture package intentionally contains no raw observations, normalized
facts, comparison results, or semantic judgements. Those artifacts are created
only by the later execution/judgement slice.

The package reserves deterministic roots for:

- `raw/`
- `normalized/`
- `comparison/`
- `judgement/`
- `cases/`
- `candidates/`
