# LT-01 input validation boundary

The fixture-design slice fixes canonical JSON inputs and answer files. Runtime
input validation belongs to the execution slice and must not manufacture S04
semantic facts.
