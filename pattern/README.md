# Pattern

This directory admits one pilot pattern at a time. Pattern selection and
semantic content are intentionally deferred until the bootstrap harness is
validated. A pattern session may not design fixtures or modify probes.
