# Runner

The thin Go runner is deferred until the workbook-native and CLI backends have
established the minimum protocol. It will provide process isolation and a
reference implementation without owning admission decisions.
