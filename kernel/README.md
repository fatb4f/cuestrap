# Kernel bootstrap surface

This directory is reserved for the minimum kernel projection required by the active pattern.

Do not import or reconstruct a general lattice kernel here. Kernel work begins only after the pilot pattern claim and its authority locators are selected. Each addition must be required by that pattern and validated by a reusable probe.
