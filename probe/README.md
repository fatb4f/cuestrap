# Probe bootstrap surface

This directory is reserved for durable, reusable CUE probe contracts and fixtures derived from the active pattern's validation requirements.

The Marimo workbook currently owns transport, backend observations, and harness validation. Promote a probe into this directory only after its semantic question and fixture shape have been designed in a probe-only or fixture-design session.
